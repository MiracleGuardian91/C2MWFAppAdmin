import { SearchColumnPipe } from './search-column.pipe';

describe('SearchColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
