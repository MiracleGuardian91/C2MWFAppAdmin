export enum Status {
  Active = '1',
  Inactive = '0',
}

export enum DMOStatus {
  Active = '1',
  Inactive = '2',
}
//Manish changes
export enum NotificationOption {
  None = 0,
  Custom_Notification = 1,  
}