import { ResizableDirectiveDirective } from './resizable-directive.directive';

describe('ResizableDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
